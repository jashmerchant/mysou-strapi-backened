{
  "kind": "collectionType",
  "collectionName": "calendars",
  "info": {
    "name": "Calendar"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "Calendar": {
      "model": "file",
      "via": "related",
      "allowedTypes": [
        "files"
      ],
      "plugin": "upload",
      "required": true
    }
  }
}
